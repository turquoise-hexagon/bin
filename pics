#!/usr/bin/env bash
#
# rename pictures based on exifs

while [[ $# -gt 0 ]]; do
    var=$(exif -m -t DateTime "$1")

    # exit if var is empty
    [[ $var ]] || exit 1

    ext=${1##*.}

    # format datas
    var=${var//:/-}
    var=${var// /_}

    mv "$1" "$var.$ext"

    shift
done
