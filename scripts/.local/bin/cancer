#!/usr/bin/env bash
#
# cancer - caNceR

while IFS= read -r line; do
    unset str

    while IFS= read -n 1 -r char; do
        ((RANDOM % 2))       \
            && str+=${char^} \
            || str+=${char,}
    done <<< $line

    printf '%s\n' "$str"
done <<< ${*-`< /dev/stdin`}
