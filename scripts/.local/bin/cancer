#!/usr/bin/env bash
#
# cancer - cAnCEr

while read -r line; do
    while IFS= read -rn1 char; do
        ((RANDOM & 2)) && echo -n "${char^}" || echo -n "${char,}"
    done <<< $line

    echo
done <<< ${*:-`< /dev/stdin`}
