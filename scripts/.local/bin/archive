#!/bin/sh
#
# archive - create tarball from file

die() {
    echo "${1:-usage : ${0##*/} [file]}" >&2
    exit 1
}

[ -e "$1" ] && {
    tar=${1%%.*}.tar.gz

    [ -e "$tar" ] &&
        die "'$tar' already exists"

    tar czvf "$tar" "$1"

    :
} ||
    die
