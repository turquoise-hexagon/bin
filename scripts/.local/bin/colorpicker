#!/bin/sh

cmd=$(command -vp colorpicker)

stdbuf -oL "$cmd" |
    while IFS=', ' read -r r g b hex; do
        printf '%s\n\033[48;2;%s;%s;%sm       \033[m\n' \
            "$hex" "$r" "$g" "$b"
    done
