#!/bin/sh
#
# colorpicker - small colorpicker wrapper

stdbuf -oL "$(command -vp colorpicker)" |
    while IFS=', ' read -r r g b hex; do
        printf '\033[48;2;%s;%s;%sm%s\033[m\n' \
            "$r" "$g" "$b" "$hex"
    done
