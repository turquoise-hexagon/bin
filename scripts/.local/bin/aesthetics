#!/usr/bin/env bash
#
# aesthetics - a e s t h e t i c s

while IFS= read -r line; do
    unset str

    while IFS= read -n 1 -r char; do
        str+="$char "
    done <<< $line

    printf '%s\n' "$str"
done <<< ${*:-`< /dev/stdin`}
