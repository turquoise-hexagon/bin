#!/usr/bin/env bash
#
# rgb -> hex

rgb=${*:-`< /dev/stdin`}

for c in $rgb; {
    printf -v out '%s%02X' "$out" "$c"
}

((${#out} != 6)) && {
    echo "usage : ${0##*/} [red] [green] [blue]" >&2
    exit 1
}

echo "#$out"
