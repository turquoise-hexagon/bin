# !/usr/bin/env bash
#
# todo script

task=$2
todo=~/.todo

pri ()
    while read -r a; do
        echo "$((c++))    $a"
    done < "$todo"

del ()
    while read -r a; do
        ((task != c++)) && echo "$a"
    done < "$todo"

case $1 in
    add) echo "$task" >> "$todo";;
    del) echo "`del`"  > "$todo";;
    *) pri
esac
