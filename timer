#!/usr/bin/env bash
#
# simple timer

# enable sleep builtin
enable -f /usr/lib/bash/sleep sleep

time=$1
text=$2

if test -z "$time"; then
    echo 'usage : [time] [text]'
    exit 1
fi

{
until [[ $(printf '%(%H:%M)T') == $time ]]; do
    sleep 10
done

notify-send "$text"
} &
