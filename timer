#!/usr/bin/env bash
#
# simple timer

time=$1
text=$2

if test -z "$time"; then
    echo 'usage : [time] [text]'
    exit 1
fi

{
    until [[ $(printf '%(%H:%M)T') == $time ]]; do
        read -rst 10 -N 1000
    done

    notify-send "$text"
} &
