#!/usr/bin/env bash
#
# pretty cmus status

# colors
a=$'\e[93m'
t=$'\e[00m'

var=$(cmus-remote -Q)

query () { echo "$var" | grep " $1 " | cut -d\  -f3-; }

cat << EOF

${a}Album${t}:    $(query album)
${a}Artist${t}:   $(query album)
${a}Title${t}:    $(query artist)

EOF
