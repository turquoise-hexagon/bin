#!/usr/bin/env bash
#
# pretty cmus status

# colors
a=$'\e[93m'
t=$'\e[00m'

query ()
{
    echo "$var" | grep " $1 " | cut -d\  -f3-
}

var=$(cmus-remote -Q)

cat << EOF

${a}Album${t}:    $(query album)
${a}Artist${t}:   $(query artist)
${a}Title${t}:    $(query title)

EOF
